#!/bin/bash

exit_code=0
capacity=$(cat /sys/class/power_supply/BAT0/capacity)
status=$(cat /sys/class/power_supply/BAT0/status)

[ "$capacity" -le  15 ] && [ "$status" = "Discharging" ] && warn="Warning: " && exit_code=1
printf "%s%s %s%%\n" "$warn" "$status" "$capacity"
exit $exit_code
