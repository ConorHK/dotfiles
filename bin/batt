#!/bin/bash

exit_code=0
capacity=$(cat /sys/class/power_supply/BAT0/capacity)
status=$(cat /sys/class/power_supply/BAT0/status)

[ "$capacity" -le 20 ] && [ "$status" = "Discharging" ] && warn="Warning: " && exit_code=1
printf "%s\t%s%s%%\n" "$status" "$warn" "$capacity"
exit $exit_code
